import React, {useState} from 'react';

const divStyle = {
    width: '100%',
    height: '100%'
}
const Table = (props) => {

    return (<div style={divStyle}>

    </div>);

};

export default Table;
